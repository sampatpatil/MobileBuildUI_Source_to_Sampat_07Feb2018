
define(
    ['kendo'// Kendo Libs
    ],

    function (K) {

        var buildRules = function () {

            var config = {
                messages: {
                },
                rules: {
                }
            };
            return config;
        };
        return buildRules;
    });